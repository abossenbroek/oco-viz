id: "4-1"
title: "Fractal Noise Module (Perlin FBM)"
wave: 4
status: done
depends: []
produces:
  - src/oco_viz/plume/noise.py
  - tests/test_noise.py
gates:
  - cmd: "pixi run ruff check src/ tests/"
    expect: exit_0
  - cmd: "pixi run mypy src/"
    expect: exit_0
  - cmd: "pixi run pyright src/"
    expect: exit_0
  - cmd: "pixi run pytest tests/test_noise.py -x"
    expect: exit_0
context:
  key_behavior: |
    Multi-octave fractal Brownian motion noise generator for volumetric detail.

    Functions:
    - fbm_3d(shape, octaves, lacunarity, gain, seed) -> NDArray[float32]
    - fbm_4d(shape, time, octaves, lacunarity, gain, seed) -> NDArray[float32]
    - curl_noise_3d(shape, ...) -> tuple of 3 NDArrays (divergence-free)

    Implementation uses scipy.ndimage.gaussian_filter at multiple scales.
  patterns:
    - "NDArray[np.float32] output"
    - "scipy.ndimage.gaussian_filter for octave generation"
  related_files:
    - src/oco_viz/plume/gaussian.py
    - src/oco_viz/plume/protocol.py
    - src/oco_viz/config/schema.py
