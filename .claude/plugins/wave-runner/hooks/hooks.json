{
  "PostToolUse": [
    {
      "matcher": "Write|Edit",
      "hooks": [
        {
          "type": "command",
          "command": "python3 \"${CLAUDE_PLUGIN_ROOT}/hooks/scripts/validate-ticket-output.py\"",
          "timeout": 15
        }
      ]
    }
  ],
  "Stop": [
    {
      "hooks": [
        {
          "type": "prompt",
          "prompt": "Check if the agent produced a valid ticket_result or gate_results YAML. If working on a ticket/gate but missing YAML output, respond {\"ok\": false, \"reason\": \"Missing required YAML output\"}. Otherwise {\"ok\": true}.",
          "timeout": 30
        }
      ]
    }
  ]
}
