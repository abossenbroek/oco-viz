# Study tier: density/TF refinement, MVP quality (base defaults apply)
sky:
  enabled: false

ground_plane:
  enabled: false

postprocess:
  fog_enabled: true
  bloom_enabled: true

motion:
  easing: smoothstep
  tempo_multiplier: 1.0

composition:
  enabled: false
