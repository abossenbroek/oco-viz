---
extends: default

ignore:
  - plan/artist_references.yaml
  - plan/artist_stand_on_shoulder.yaml

rules:
  line-length:
    max: 120
  truthy:
    allowed-values: ["true", "false", "on"]
  comments:
    min-spaces-from-content: 1
  document-start: disable
